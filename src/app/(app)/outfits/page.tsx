import { Placeholder } from "@/components/placeholder";

export const metadata = { title: "Outfits · fitr" };

export default function OutfitsPage() {
  return (
    <Placeholder
      title="Outfits"
      phase="Phase 2"
      description="A drag-and-layer canvas over your cutouts, where a slot can hold a piece you own, a piece you want, or a gap describing something you still need."
    />
  );
}
