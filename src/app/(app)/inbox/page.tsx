import { Placeholder } from "@/components/placeholder";

export const metadata = { title: "Inbox · fitr" };

export default function InboxPage() {
  return (
    <Placeholder
      title="Inbox"
      phase="Phase 4"
      description="Everything you saved from TikTok, Instagram, or the web — waiting to be turned into a want, or dropped straight into an outfit's empty slot."
    />
  );
}
