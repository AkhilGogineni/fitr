import { Placeholder } from "@/components/placeholder";

export const metadata = { title: "Today · fitr" };

export default function TodayPage() {
  return (
    <Placeholder
      title="Today"
      phase="Phase 3"
      description="What to wear, from what you own, given the forecast and the occasion. Confirming an outfit here is what quietly builds your wear history."
    />
  );
}
